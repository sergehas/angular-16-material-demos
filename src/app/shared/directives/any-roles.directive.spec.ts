import { AnyRolesDirective } from './any-roles.directive';

describe('IsAuthorizedDirective', () => {
  it('should create an instance', () => {
    const directive = new AnyRolesDirective();
    expect(directive).toBeTruthy();
  });
});
